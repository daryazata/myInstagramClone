import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 10,

    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#ff0000',
  },
  image: {
    borderRadius: 40,
    borderWidth: 3,
    borderColor: 'white',
  },
});
export default styles;
